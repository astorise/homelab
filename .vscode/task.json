{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo build homelab-tauri",
      "type": "shell",
      "command": "cargo build -p homelab-tauri",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": ["$rustc"]
    }
  ]
}